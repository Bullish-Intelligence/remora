from grail import Input

file_name: str = Input("file_name", default="new_file.txt")

try:
    result = {
        "file_name": file_name,
        "created": True,
    }
except Exception as exc:
    result = {"error": str(exc)}

result
