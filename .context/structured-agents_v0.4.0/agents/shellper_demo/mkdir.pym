from grail import Input

dir_name: str = Input("dir_name", default="new_folder")

try:
    result = {
        "dir_name": dir_name,
        "created": True,
    }
except Exception as exc:
    result = {"error": str(exc)}

result
