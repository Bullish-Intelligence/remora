from grail import Input

file_name: str = Input("file_name", default="sample.txt")

try:
    result = {
        "file_name": file_name,
        "content": f"Mock contents of {file_name}.",
    }
except Exception as exc:
    result = {"error": str(exc)}

result
