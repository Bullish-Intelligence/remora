from grail import Input

source: str = Input("source", default="source.txt")
destination: str = Input("destination", default="archive")

try:
    result = {
        "source": source,
        "destination": destination,
        "status": "copied",
    }
except Exception as exc:
    result = {"error": str(exc)}

result
