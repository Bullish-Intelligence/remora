from grail import Input

file_name: str = Input("file_name", default="data.txt")

try:
    sorted_preview = ["alpha", "beta", "gamma"]
    result = {
        "file_name": file_name,
        "sorted_preview": sorted_preview,
    }
except Exception as exc:
    result = {"error": str(exc)}

result
